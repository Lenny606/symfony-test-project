{#    in start can be action route configured #}
{{ form_start(form, {'attr': {'class': 'max-w-md mx-auto p-4 bg-white shadow-lg rounded'}}) }}

    {{ form_errors(form) }}

<div class="mb-4">
    {{ form_label(form.title, 'Add Event title', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.title, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>

<div class="mb-4">
    {{ form_label(form.content, 'Add Event Content', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.content, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>

<div class="mb-4">
    {{ form_label(form.date, 'Add Event Date', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.date, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>
<!-- Optional Fields Toggle Checkbox -->
<div class="mb-4">
    <label class="block text-gray-700 font-bold mb-2" for="toggleOptionalFields">Show Optional Fields</label>
    <input type="checkbox" id="toggleOptionalFields">
</div>
<div class="mb-4  optional-field" >
    {{ form_label(form.contact.firstName, 'Add Name', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.contact.firstName, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>
<div class="mb-4  optional-field" >
    {{ form_label(form.contact.surname, 'Add Name', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.contact.surname, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>
<div class="mb-4  optional-field" >
    {{ form_label(form.contact.email, 'Add Name', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.contact.email, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>
<div class="mb-4  optional-field" >
    {{ form_label(form.contact.phone, 'Add Name', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.contact.phone, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>

<button type="submit" class="w-full mt-4 px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-500 focus:outline-none focus:bg-indigo-500">Save Event</button>
<script>
    // JavaScript code here
    document.addEventListener("DOMContentLoaded", function() {
        var checkbox = document.getElementById("toggleOptionalFields");
        var optionalFields = document.querySelectorAll(".optional-field");

        checkbox.addEventListener("change", function() {
            if (checkbox.checked) {
                optionalFields.forEach(function(field) {
                    field.style.display = "block";
                });
            } else {
                optionalFields.forEach(function(field) {
                    field.style.display = "none";
                });
            }
        });

        // Initially hide optional fields if the checkbox is not checked
        if (!checkbox.checked) {
            optionalFields.forEach(function(field) {
                field.style.display = "none";
            });
        }
    });
</script>
{{ form_end(form) }}


{# old form control #}
{#    {{ form(form) }} #}