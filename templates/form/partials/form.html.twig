{#    in start can be action route configured #}
{{ form_start(form, {'attr': {'class': 'max-w-md mx-auto p-4 bg-white shadow-lg rounded'}}) }}

    {{ form_errors(form) }}

<div class="mb-4">
    {{ form_label(form.title, 'Add Post title', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.title, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>

<div class="mb-4">
    {{ form_label(form.text, 'Add Post Content', {'class': 'block text-gray-700 font-bold mb-2'}) }}
    {{ form_widget(form.text, {'attr': {'class': 'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:border-indigo-500'}}) }}
</div>

<button type="submit" class="w-full mt-4 px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-500 focus:outline-none focus:bg-indigo-500">Save Post</button>

{{ form_end(form) }}


{# old form control #}
{#    {{ form(form) }} #}