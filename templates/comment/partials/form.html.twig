{#    in start can be action route configured #}
{{ form_start(form, {'attr': {'class': 'mt-8'}}) }}

<div class="mb-4">
    {{ form_errors(form) }}
    {{ form_label(form.text, 'Add Comment', {'class': 'block text-sm font-medium text-gray-700'}) }}
    {{ form_widget(form.text, {'attr': {'class': 'mt-1 p-2 block w-full border border-gray-300 rounded-md focus:outline-none focus:ring-indigo-500 focus:border-indigo-500'}}) }}
</div>

<button type="submit" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">Save Comment</button>
<a href="{{ path('app_micro_post_show', {id: post.id}) }}" class="mt-4 inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
    Cancel
</a>

{{ form_end(form) }}


{# old form control #}
{#    {{ form(form) }} #}