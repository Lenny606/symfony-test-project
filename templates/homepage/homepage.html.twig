<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        {{ '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16">
        <path d="M8 0a8 8 0 0 1 8 8c0 4.418-3.582 8-8 8a8 8 0 0 1-8-8c0-4.418 3.582-8 8-8zm-.5 3h-1v1h1a3 3 0 0 1 3 3v.5h1v-1h-.5a1 1 0 0 0-1 1v1a3 3 0 0 1-3-3zm1 6a1 1 0 0 0 1-1v-.5h-1v1h-.5a1 1 0 0 0-1 1v.5h1zm.5 1h-1v1h1a3 3 0 0 1 3-3v-.5h-1v1h.5a1 1 0 0 0 1-1v-1a3 3 0 0 1-3 3z"/>
        </svg>'|raw }}
        {{ pageTitle }}
    </title>
    <!-- Include your CSS stylesheets here -->
    {% block stylesheets %}
    {% endblock %}
</head>
<body>
<header>
    <h1>Welcome to Our Website</h1>
    <nav>
        <ul>
            <li><a href="{{ path('app.index') }}">Home</a></li>
            <li><a href="{{ path('app.show', {id: id}) }}">Show Us</a></li>
            {#            <li><a href="{{ path('about_page') }}">About Us</a></li> #}
            <!-- Add more navigation links as needed -->
            <li><a href="{{ path('app_micro_post_add') }}">New Post</a></li>
        </ul>
    </nav>
</header>

<main>
    <section>
        <h2>Featured Products</h2>

        {#        passing variables pfrom paraent to child #}
        {{ include('homepage/partials/productList.html.twig', {featuredProducts: featuredProducts}) }}

    </section>

    <section>
        <h2>Latest News</h2>
        <div class="news-list">
            {#            {% for article in latestNews %} #}
            {#                <article> #}
            {#                    <h3>{{ article.title }}</h3> #}
            {#                    <p>{{ article.summary }}</p> #}
            {#                    <a href="{{ path('article_details', { 'id': article.id }) }}">Read more</a> #}
            {#                </article> #}
            {#            {% endfor %} #}
        </div>
    </section>
</main>

<footer>
    <p>&copy; {{ currentYear }} Our Website. All rights reserved.</p>
</footer>

<!-- Include your JavaScript scripts here -->
{% block javascripts %}
    {% block importmap %}{{ importmap('app') }}{% endblock %}
{% endblock %}
</body>
</html>
